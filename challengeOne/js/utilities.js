function activeFilter(todos) {
    // filter through todos and return those not completed
}

function completedFilter(todos) {
    // filter through todos and return completed
}

export default {
    activeFilter,
    completedFilter
    // shortcut syntax above
    // "activeFilter": activeFilter
}